const Login = () => {
    return (
        <div>
            <h1>Login</h1>
            <button onClick={()=>window.location.href="index"}>Login</button>
        </div>
    );
}

export default Login;